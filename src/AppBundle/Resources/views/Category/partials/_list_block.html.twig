<ul class="list-group">
    {% for item in items %}
        <li class="list-group-item"><a href="{{ path('category_products', {category_name: item.slug }) }}">{{ item.name }}</a></li>
    {% endfor %}
</ul>